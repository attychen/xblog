---
title: "MarketNow：实时MCP拦截器，阻断.env读取与危险代理命令"
date: "2026-08-09"
excerpt: "MarketNow 是 MCP 服务器的 npm 平台，提供 9,248 个 MCP 服务器，适用于 Claude Desktop、Cursor、Cline、Continue、Aider 以及任何兼容 MCP 的代理。完全免费，所有技能均免费。经过 Sentinel L2.5 安全审计（gVisor 沙箱）。AliceLabs LLC 专有。查找 MCP 服务器，一条命令即可安装。为您的代理栈添加"
category: "ai-tools"
tags: ["MCP服务器","安全审计","AI代理","Claude Desktop","开发者工具"]
original: "https://marketnow.site/"
draft: false
---

## 1. MarketNow 概览

MarketNow 是 MCP 服务器的 npm 平台，提供 9,248 个 MCP 服务器，适用于 Claude Desktop、Cursor、Cline、Continue、Aider 以及任何兼容 MCP 的代理。完全免费，所有技能均免费。经过 Sentinel L2.5 安全审计（gVisor 沙箱）。AliceLabs LLC 专有。查找 MCP 服务器，一条命令即可安装。为您的代理栈添加能力的最快方式。按类别、标签或关键词浏览 9,248 个 MCP 服务器。按价格、Sentinel 评分或语言筛选。查看真实的 GitHub 星标、真实的 npm 下载量、真实的 Sentinel 报告。安装命令：npx -y @marketnow/install slug。

## 2. 开发者使用流程

1. 搜索：按类别、标签或关键词浏览 9,248 个 MCP 服务器。按价格、Sentinel 评分或语言筛选。查看真实的 GitHub 星标、真实的 npm 下载量、真实的 Sentinel 报告。2. 支付（或获取免费）：通过 Stripe 使用信用卡支付（享有完全退款权利）或通过 Base L2 使用 USDC。43 项技能免费——无需付款，无需注册，直接安装。3. 安装：npx -y @marketnow/install slug——适用于 Claude Desktop、Cursor、Cline、Continue、Aider 以及任何兼容 MCP 的运行时。

## 3. 热门 MCP 服务器类别

AI/ML——1,976 项技能（LLM 封装器、嵌入、RAG、视觉）；开发者工具——1,891 项技能（代码分析、代码检查、构建工具）；数据——782 项技能（数据库、ETL、分析）；Web/API——348 项技能（HTTP 客户端、REST、GraphQL）；生产力——328 项技能（任务管理、笔记、日历）；金融——292 项技能（支付、会计、加密货币）；通信——288 项技能（Slack、Discord、电子邮件）；安全——224 项技能（认证、扫描、机密管理）。

## 4. 43 项免费 MCP 技能

真实价值，零成本。43 项精心挑选的技能可供即时下载。无需信用卡、无需强制要求、无需注册。只需安装即可使用。浏览全部 9,248 项免费技能。

## 5. 面向 AI 代理的 API

一切机器可读。公开 JSON API。默认采用人在环路（Human-in-the-loop）。GET /api/skills.json——完整目录（9,248 项技能，JSON，约 24MB）；GET /api/search?q=——带相关性评分的服务端搜索；GET /api/agent.json——机器可读的指令、信任模型、模式；GET /api/policies.json——条款、退款、争议、隐私政策；POST /api/agent-purchase——购买技能（5 种模式：instant_download、instant_purchase、direct_purchase、requires_payment、requires_human_approval）；GET /api/mandates——委托授权（符合 ACP/AP2）；GET /api/free-skills.json——9,248 项免费技能；GET /api/bundles.json——折扣技能包。MCP 服务器：npx -y marketnow-mcp——适用于 Claude Desktop、Cursor、Cline。可在 npm 和 Smithery 上获取。

## 6. 用户推荐理由

AliceLabs 专有——完整审计方法在 GitHub 上公开。每次更改都是公开提交。没有黑箱。Sentinel L1.5——对每项技能进行 6 点 MCP 安全审计。方法已发布，可重新运行。43 项免费技能——真实价值，零成本。无需注册、无需信用卡、无需强制要求。$0.99–$9.99 一次性付费——无订阅、无按次调用费用、无分级计划。一次付费，永久拥有。默认人在环路——每次购买时，授权都会通知委托人。静默模式需要明确选择加入。公开审计日志——每笔授权交易都是 _data/mandates/ 下的一个 git 提交。完全可审计。5 种语言——系统提示支持英语、西班牙语、中文、葡萄牙语、法语。真实翻译，非自动翻译。诚实的路线图——/trust 页面如实说明已完成、部分完成和待办事项。没有虚假的“已验证”徽章。

## 7. 信任与透明度

/trust——公开信任路线图（Claude AI 审核员提出的 7 点）；/about——关于我们（AliceLabs LLC，美国怀俄明州，创始人 Edison Flores）；/catalog——9,248 项技能的来源（目录透明度）；/policies——条款、退款、争议政策（包括 USDC 争议流程）；/mandates——管理支出授权（默认人在环路）；/embed——可分享徽章（Powered by MarketNow, Sentinel L2.5）；/pricing——面向卖家的定价层级；/security——Sentinel L1.5 方法；/handshake——代理信任握手协议。

## 8. 审核状态披露与项目信息

在 9,248 项技能中：8,742 项为自动扫描（Sentinel 已运行，但未经人工审核），22 项为人工审核（精选免费技能），0 项为维护者验证（该计划尚未启动）。我们绝不未经努力就声称“已验证”。由 AliceLabs LLC 维护，2024 年由 Edison Flores 创立（AliceLabs LLC，美国怀俄明州）。无投资者，除创始人外无员工，无营销预算。公开建设，每次更改都是可见的 git 提交，地址为 github.com/edgarfloresguerra2011-a11y/marketnow。支持语言：英语、西班牙语、中文、葡萄牙语、法语。关键词：MCP 服务器、模型上下文协议、MCP 市场、MCP 目录、Claude Desktop 技能、Cursor MCP、Cline MCP、代理技能、Sentinel 安全审计、Smithery、Anthropic MCP、开源 MCP、MCP 服务器注册表、安装 MCP 服务器。

---

🔗 **原文链接：[https://marketnow.site/](https://marketnow.site/)**
